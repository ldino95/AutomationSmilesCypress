{
  "scripts": {
    "test:chrome": "cypress run --brower chrome --no-exit"
  },
  "cypress-cucumber-preprocessor": {
    "step_definitions": "cypress/support/steps"
  },
  "devDependencies": {
    "@shelex/cypress-allure-plugin": "^2.9.0",
    "cypress": "^8.2.0",
    "cypress-cucumber-preprocessor": "^4.2.0",
    "mochawesome": "^6.2.2"
  }
}
